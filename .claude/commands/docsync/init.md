# /docsync:init

Use the project Skill `.claude/skills/docsync/SKILL.md`.

Run the DocSync `init` flow with the user arguments:

`$ARGUMENTS`

Follow the Skill safety rules and report format exactly.
