# /docsync:sync

Use the project Skill `.claude/skills/docsync/SKILL.md`.

Run the DocSync `sync` flow with the user arguments:

`$ARGUMENTS`

If `--fast` is in arguments, use the fast sync flow.
If file names are provided, only sync those files.

Follow the Skill safety rules and report format exactly.
