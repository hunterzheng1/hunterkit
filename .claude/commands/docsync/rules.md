# /docsync:rules

Use the project Skill `.claude/skills/docsync/SKILL.md`.

Run the DocSync `rules` flow with the user arguments:

`$ARGUMENTS`

If `show` is in arguments, display the full override rules.
Otherwise, ask the user what rule to add or modify.

Follow the Skill safety rules and report format exactly.
