# T5 — 迁移/权限变更（full 档）

- **起始 commit**：`898c8ed3bbc1945b7a86e3641727a7342426356e`（干净树）
- **需求原文**：`runtime/invalidations/` 回执（E1 引入，schemaVersion 1）增加
  可选字段 `triggerPhase`（触发失效的阶段名）。要求：旧回执（无该字段）读取
  不报错、语义不变；新写入包含该字段；提供旧/新双 fixture 的兼容读取测试。
- **验收条件（对实现者可见）**：
  1. 旧 fixture（E1 测试产出的形态）读取行为与变更前完全一致；
  2. 新写入的回执含 `triggerPhase`；
  3. 兼容测试含旧/新双 fixture；`test_harness_fixback.py` 全绿。
- **禁止事项**：不删除旧字段；不引入版本协商机制（单可选字段不需要）。
- **超时预算**：120 分钟墙钟。
- **独立验收保留项**：评估侧验证「缺字段≠损坏」：损坏 JSON 仍走原有失败路径，
  不得因兼容逻辑放宽。
- **风险档位**：full（状态 schema 变更 + 兼容读取，含 migration 信号）。
