# 批次 1 试点采集记录：组 3（候选方案 harness_task.py）T3 普通功能 · 第 2 次运行（可重复性验证）

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch1-pilot-clones\g3-t3-r2`（fresh clone @ 898c8ed + env 提交，detached HEAD）
> 任务：T3（同 r1）
> 运行身份：runId `task_cae38f27-fa6f-41fa-8a15-475679428878`，change `batch0-g3-t3-status-field`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：与 r1 逐字节一致（harness_change.py 与 test 文件 FULL-DIFF-IDENTICAL）；46/46 绿；独立保留检查通过 |
| 提交 | `2495767`（2 文件 +117 行）+ `9e5b3ca`（bundle hash sync，见 P9） |
| 归档 | `.harness/archive/2026-09-07-batch0-g3-t3-status-field`（record-only） |
| 档位 | standard（自动） |
| 端到端墙钟 | **30.46 min**（含 2 次失败重试的完整排障；纯成功路径 ~8.6 min） |
| finish 成功执行 | 8.35 min（500,885 ms） |
| 失败重试 | 2 次：① flaky 测试超时（非任务相关）；② FOREIGN_PATHS_PRESENT（P9，见下） |

## 2. 命令级计时

| 步骤 | 耗时 |
|---|---|
| harness_task.py begin | 0.79 s |
| finish 尝试 1（VERIFICATION_FAILED） | 833.7 s（验证跑了 13.9 min 后挂在 flaky 测试） |
| finish 尝试 2（FOREIGN_PATHS_PRESENT） | 1.1 s |
| finish 尝试 3（成功） | 500.9 s 总计，其中验证 498.1 s |
| **流程维护净耗时（成功那次）** | **~2.8 s** |
| **流程维护合计（begin + 成功 finish 净）** | **~3.6 s** |

## 3. 失败与摩擦明细

| # | 事件 | 原因 | 解决 |
|---|---|---|---|
| 1 | finish 失败 `VERIFICATION_FAILED`（unitTestFull exit 1） | `packages/core/test/recovery-v3.test.ts`「100 durable recoveries」压力测试 120 s 超时——**flaky，与任务变更无关**（r1 同链通过；2242/2243 通过仅此 1 挂） | 重跑 finish |
| 2 | finish 失败 `FOREIGN_PATHS_PRESENT`（hunter-workflow-family.json） | **P9**：尝试 1 的 npm test pretest 副作用（sync:harness 改 bundle hash）发生在 ownership 契约生成**之后**；重试时该文件被判定为「begin 后新出现的边界外路径」。实际是合法任务工作（r1 中同样文件被包含在任务提交里） | 按工具指引手工提交该文件后重跑成功 |

## 4. r1 vs r2 对照（可重复性结论）

| 指标 | r1 | r2 | 差异 |
|---|---|---|---|
| 端到端墙钟 | 21.52 min | 30.46 min（纯成功路径 ~8.6 min） | 重试放大 |
| 失败重试 | 0 | 2（1 flaky + 1 P9） | +2 |
| 产出 | +118 行 | +117 行（逐字节一致；bundle hash 因提交拆分少 1 行） | 0 |
| 档位 | standard | standard | 0 |
| 流程维护 | ~7.4 s | ~3.6 s | 均极小 |

**结论**：产出可重复性成立（核心文件逐字节一致）。两次重试都不是契约不可发现问题：flaky 测试是仓库既有问题；P9 是候选方案对「验证副作用文件」的边界判定过严，有明确修复方向。

## 5. 新增发现

| # | 发现 | 类型 |
|---|---|---|
| P9 | **finish 重试时验证副作用文件被误判 foreign**：第一次 finish 的验证（npm pretest → sync:harness）会改写 bundle manifest hash；该变更发生在 ownership 契约快照之后，重试 finish 时被 FOREIGN_PATHS_PRESENT 拒绝。修复方向：finish 失败重入时重新生成 ownership 契约，或把验证副作用路径纳入白名单 | 候选方案缺陷 |
| P10 | **flaky 测试会放大候选方案的墙钟**：验证链全量跑（2243 tests）时单个 120 s 超时 flaky 即导致整个 finish 失败重来（13.9 min 白跑）。g2 的定向验证（46 tests）天然免疫。P5/P6 修复（去重+定向）同时缓解此问题 | 候选方案改进点 |
| P11 | r2 验证耗时（498 s）比 r1（679 s）低 27%——npm 链耗时波动大（vitest 并行度/缓存状态），单次样本的验证时间不可直接比较 | 观察 |

## 6. 与方法学 §4 的映射

| 维度 | 本次数据 |
|---|---|
| 结果 | 验收通过（含独立保留检查）；0 缺陷；0 返工 |
| 时间 | 端到端 30.46 min（含重试）；纯成功路径 ~8.6 min；流程维护 ~3.6 s |
| 成本 | 未采集 |
| 摩擦 | 2 次失败（1 flaky 仓库既有 + 1 P9 候选缺陷）；0 契约不可发现 |
| 验证 | 尝试 1 的 13.9 min 验证白跑（P10）；同链三连跑延续（P5） |
| 资产 | knowledge_candidates 1；ledger 3 条（成功尝试）+ 1 条失败记录 |
