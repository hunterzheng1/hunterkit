# 批次 1 试点采集记录：组 3（候选方案 harness_task.py）T2 状态输出扩展 · 第 1 次运行

> 日期：2026-09-08
> 工作区：`E:\MyProject\AI Related\batch1-pilot-clones\g3-t2-r1`（fresh clone @ 898c8ed + env 提交，detached HEAD）
> 任务：T2（`docs/harness-improvement-roadmap/batch0/tasks/T2-status-summary.md`）：`harness_change.py status` 的 summary 增加 `total` 字段（区分「目录不存在」与「有 change 但分类失败」）+ 回归测试
> 候选方案：`harness_task.py begin/finish/status` 轻任务闭环（efb6f13：含 P1/P5/P6 验证计划）
> 运行身份：change `batch0-g3-t2-status-total`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：`cmd_status` summary 增加 `total = len(items)`（dict 展开，空集 0 / 非空 len）；3 个新测试（空=0 / 非空=len / INVALID-only 一致）；套件 45/45 绿 |
| 提交 | `87048f2`（3 文件：harness_change.py +21/-9、test_harness_change.py +60、hunter-workflow-family.json 快照更新） |
| 归档 | `.harness/archive/2026-09-08-batch0-g3-t2-status-total`（record-only） |
| 档位 | standard（classify 自动判定，符合任务定义） |
| 端到端墙钟 | ~11 min（begin → 归档完成；含实现与测试编写） |
| finish 成功执行 | 4.12 min（247,096 ms） |
| 失败重试 | 0 次（begin/finish 均一次成功） |

## 2. 命令级计时

### 流程维护命令（协调开销，§4 口径）

| 步骤 | 耗时 |
|---|---|
| harness_task.py begin | ~1 s |
| harness_task.py finish（成功） | 247.1 s 总计，其中验证 242.6 s |
| **流程维护净耗时（finish − 验证）** | **~4.5 s** |
| **流程维护合计** | **~5.5 s** |

### 验证命令（不计入流程维护）

| 验证项 | 解析为 | 耗时 |
|---|---|---|
| compile | unitTestFull（npm lint+typecheck+test+build 链，fallback） | 237.4 s |
| unitTest | **定向 python -m unittest test_harness_change（python-targeted）** | 5.1 s |
| unitTestFull | DEDUPED（dedupedFrom: compile） | 0 s |
| **验证合计** | | **242.6 s（4.04 min）** |

P1/P5/P6 修复效果第 4 次复现（T1-r3、T3-r3、T5-r1 之后）：验证从修复前
预期的 3× npm 链（~712 s）降为 1× npm 链 + 1× 定向 python（242.6 s，-66%）。
ledger 实证定向路径：`unitTest` 条目 inputsFiles 精确为
`[harness_change.py, test_harness_change.py]`（explicit-files，incremental）。

## 3. 关键发现

### 3.1 P13（轻微）：归档 decision 事件的 tier 措辞误导

归档事件流出现 `decision | review missing on full tier (allowed by user)`，
但本任务 tier=standard。根因：`harness_task.py finish` 对轻任务归档固定传
`allow_missing_review=True`（harness_task.py:1387，轻任务流程无 review 阶段），
而 `harness_archive.py:8522` 的 note 文案硬编码 "full tier"——该文案在
standard 档下照常发出，读事件流的人会误以为发生了 full 档越权。

- 影响：仅事件流可读性（审计时需人工核对真实 tier）；无行为差异
  （standard 档本就不要求 review，`--allow-missing-review` 对它是空操作）。
- 修复方向：note 文案带上实际 tier（如 `review missing on {tier} tier`）或
  仅在 `risk_tier == "full"` 时发该 decision 事件。一行改动 + 事件断言更新。

### 3.2 T2 任务本身（观测点验证）

任务设计意图「区分目录不存在 vs 有 change 但分类失败」达成：实现者选择
`{"total": len(items), **{status: count}}` 的 dict 展开序（total 键在前，
不覆盖状态计数），并补了 INVALID-only 边界（全 INVALID 时 total=len 且
INVALID=len，调用方可识别异常态）。实现者未引入额外抽象（遵守禁止事项）。

## 4. 与 g2（当前 Harness）对照

| 指标 | g2（基线，无 T2 样本） | g3-r1（本运行） |
|---|---|---|
| 端到端墙钟 | 未采集（T2 未跑 g2） | ~11 min |
| 流程维护 | 未采集 | ~5.5 s |
| 档位判定 | 手工 configure-plan | 自动 standard（正确） |

## 5. 备注

- 本运行是 P1/P5/P6 修复（efb6f13）后的第 4 次端到端验证，定向验证与
  去重行为连续 4 次稳定复现（T1-r3 661ms doc-contract、T3-r3/T5-r1/T2-r1
  python-targeted）。
- hunter-workflow-family.json 快照随提交更新（P9 副作用路径，与 T3-r1
  观察一致）：归档清单变更正确合并进产品提交，无人工干预。
