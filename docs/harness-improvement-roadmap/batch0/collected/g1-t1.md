# 批次 0 采集记录：组 1（原生 Agent）T1 文档修改 · 2 次运行

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch0-clones\g1-t1-r1`、`g1-t1-r2`（fresh clone @ 898c8ed，detached HEAD，**无 harness init**）
> 对照口径：不加载任何 Harness Skill、无流程命令；agent 直接探索 → 编辑 → 测试 → 提交。计时为会话级墙钟（clone 完成 → git commit 完成）。
> 知识状态：与组 2 r2 同等热缓存（同会话、任务契约已知）——这是公平对照的前提。

## 1. 结果总览

| 运行 | 端到端墙钟 | 步骤 | 失败 | 提交 | 验收 |
|---|---|---|---|---|---|
| r1 | **1.42 min**（85.2 s） | grep 定位 → 读上下文 → Edit ×2 → doc contract 8/8 → commit | 0 | `76a3d07` | 通过 |
| r2 | **0.82 min**（49.0 s） | 同上（定位路径已知） | 0 | `62e5bb9` | 通过 |

两次产出逐字节一致（2 行 diff，仅触及两处「10 项」→「12 项」）。

## 2. 独立验收（评估侧保留项）

- 范围外文件 diff：空（两次运行均只改 SKILL.md）。
- `coding-reference.md`/`testing-reference.md` 中的「10 项」表述未被顺手修改（3 处保留）。
- 新表述与 `testing-checklist.md` 实际条目数一致（12）。

## 3. 观察要点

1. **零失败**：没有契约可违反——没有 plan-evidence-input、没有 ledger、没有 gate。全部时间是探索 + 编辑 + 验证。
2. **r1→r2 也有学习效应**（-42%），但绝对值小（1.42→0.82 min）；组 2 的学习效应是 49.6→5.41 min（-89%）。原生组的「学习」只是记住文件路径；Harness 组的学习是整套字段契约。
3. **流程维护时间 = 0**（方法学 §4 口径：原生组无流程）。组 2 稳态的流程维护（~32 s 命令 + ~1.5 min 仪式写作）在原生组完全不存在。

## 4. 与方法学 §4 的映射

| 维度 | r1 / r2 |
|---|---|
| 结果 | 验收通过（含独立验收保留项）；0 缺陷；0 返工 |
| 时间 | 端到端 1.42 / 0.82 min；流程维护 0 |
| 成本 | 未采集 |
| 摩擦 | 0 失败；0 人工介入 |
| 验证 | doc contract 8/8（两次均跑）；测试漏选 N/A（文档任务） |
| 资产 | 无（原生组不产生知识候选——对照点：Harness 组每次产出 knowledge_candidates） |
