# 批次 0 采集记录：组 2（当前 Harness）T1 文档修改 · 第 1 次运行

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch0-clones\g2-t1-r1`（fresh clone @ 898c8ed，detached HEAD）
> 任务：T1（`docs/harness-improvement-roadmap/batch0/tasks/T1-docs-edit.md`）
> 运行身份：runId `plan_1f41142a-7442-40da-87f0-dcec64272197`，attempt 1，change `batch0-g2-t1-docs-fix`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：两处「10 项」→「12 项」；doc contract 8/8；diff 仅 2 行；范围外零 diff |
| 提交 | `22a3d89`（detached HEAD，未 push——clone 无 remote 语义） |
| 归档 | `.harness/archive/2026-09-07-batch0-g2-t1-docs-fix`（record-only） |
| 端到端墙钟 | 49.6 min（15:18:41 → 16:08:16，含人工等待与排障） |
| 活动执行（harness 口径） | 32.26 min（plan 17.13 + execute 12.25 + archive 2.88） |
| 人工等待 | ~17.3 min（模型思考/工具间隙，agentOrToolUnattributedMs 1,317,938 ms） |

## 2. 命令级计时（模型手工计时，`time` 输出）

### 环境准备（非任务工作，不计入流程维护）

| 步骤 | 耗时 | 备注 |
|---|---|---|
| fresh clone + checkout 898c8ed | 0.2 s | `--no-checkout` 后 checkout |
| harness init（成功） | 3.1 s | 114 items；`--profile general` |

### 流程维护命令（协调开销，§4 口径）

| 步骤 | 耗时 | 失败重试 |
|---|---|---|
| bootstrap-plan | 0.94 s | 0（参数摸索 2 次：--project 取 `.` 而非项目名，0.16+0.26 s） |
| configure-plan | 0.17 s | 1（缺 --operator，0.15 s） |
| plan publish（最终成功） | 1.94 s | 3 次（见 §3 摩擦 F2/F3） |
| gate close plan（含 claim） | 0.15+0.37 s | 1（LEASE_ABSENT→claim，0.28 s） |
| context handoff ×2 | 0.25+0.38 s | 0 |
| gate begin execute | 0.79 s | 2（CONTEXT_HANDOFF_REQUIRED ×2，0.33+0.33 s） |
| gate begin archive | 0.43 s | 1（CONTEXT_HANDOFF_REQUIRED，0.35 s） |
| ledger record ×4 | 0.74+0.35+0.35+0.36 s | 4（见 §3 摩擦 F4） |
| gate close execute | 10.4 s | 1（VALIDATION_MISSING→补 compile/unitTest） |
| archive status（预检） | 0.57+0.62 s | 1（release-candidate→record-only） |
| archive execute | 1.33 s | 0 |
| git commit | 0.06 s | 0 |

**流程维护命令累计：~18.5 s（纯命令执行）**
**流程维护失败重试累计：~2.6 s 命令时间 + 8 次失败往返的模型排障时间（未计入，估计 20-40 min 模型时间）**

### 验证命令（不计入流程维护）

| 步骤 | 耗时 |
|---|---|
| doc contract test（裸跑确认） | 1.19 s |
| doc contract test（test_runner exec） | 0.55 s |
| preflight detect | ~0.5 s |

### 模型手工产物（仪式写作）

| 产物 | 估时 |
|---|---|
| plan-evidence-input.json（含模板学习） | ~12 min（含 3 次失败修正） |
| diff 验证与场景核对 | ~2 min |

## 3. 摩擦发现（评估侧记录）

| # | 发现 | 类型 | 证据 |
|---|---|---|---|
| F1 | **worktree 内 quick 档默认阶段不可发布**：git-probe 探测 uses_worktree=true → configurePlannedPhases 强制 merge 必需（configuration.ts:99），`plan,execute,archive` 省略 merge → not_publishable → PLANNING_CONTEXT_INVALID（module.ts:343），错误无 field_path | 工具缺陷+可观测性 | 复现脚本 repro.mjs；worktree 实测 3 次失败 |
| F2 | **本地未连接项目 project_id=null 使 finalize 必败**：validContext 要求非空 ID（module.ts:183），但本地 init 的 project.yaml project_id 恒为 null；e2e 测试用任意 `prj_*` 字符串即可通过——契约未文档化 | 工具缺陷+文档缺口 | plan-evidence-input.json project_id null→失败，`prj_batch0-g2-t1-r1`→成功 |
| F3 | **plan publish 错误信封缺定位字段**：PLANNING_CONTEXT_INVALID / PLAN_FINALIZATION_INPUT_INVALID 均无 field_path，需读源码定位（本次排障 ~25 min） | 可观测性 | 两次错误输出 |
| F4 | **ledger record 参数契约陡峭**：4 次失败（verification 枚举、--files 与 profile 冲突、status 枚举 PASS→OK、profile-input default→unitTestFull）；错误信息质量好但每次都要试错 | 摩擦 | 4 次失败记录 |
| F5 | **classify 不识别 docs-only 为 fast**：bootstrap-plan 判 standard（tierSource: default-standard），需手工 configure-plan 降档；docs-only 信号在 evidence-pack 才被推断（signal_provenance: inferred） | 工具改进点 | bootstrap 输出 |
| F6 | **execute 关门要求 compile/unitTest/unitTestFull 三项**：docs-only 变更也须三项 ledger 记录（复用同一测试命令）；且 build-profile 只有 unitTestFull 一个 target，compile/unitTest 无对应 profile-input 语义 | 工具改进点 | gate close VALIDATION_MISSING |
| F7 | **场景覆盖 WARN**：DOC-002/DOC-003（diff_review 类场景）无 ledger 记录途径——diff review 是人工/模型行为，ledger verification 枚举无对应项；lenient 模式降 WARN 放行 | 工具缺口 | gate close 输出 |
| F8 | **deployed bundle 滞后于仓库源码**：bundle 0.2.80（2026-09-06 22:32）不含 E2（898c8ed，09-07）；init 部署的是 bundle 而非仓库源——在仓库上验证过的修复要等下个 bundle 才生效 | 发布流程 | bundle hash 对比 |

## 4. harness 自动采集 vs 手工记录

- `timing.attempts` 完整捕获三阶段（plan 17.13 min / execute 12.25 min / archive 2.88 min，active-only 口径）。
- `efficiency` 段全零：wallClockMs/activeTimeMs/timingByStage 均为 0——该段依赖 run-session 事件（stageTimings），本次运行未产生（命令直接调用而非 skill 会话内调用）。**采集链路缺口：命令级调用不产生 efficiency 数据。**
- 人工等待（agentOrToolUnattributedMs 21.97 min）与模型排障时间只能评估侧手工记录。

## 5. 与方法学 §4 的映射

| 维度 | 本次数据 |
|---|---|
| 结果 | 验收通过；0 严重缺陷；0 返工 |
| 时间 | 端到端 49.6 min；活动 32.26 min；流程维护命令 ~18.5 s + 排障 ~25 min（F1-F4 排障占大头） |
| 成本 | 未采集（无 token 计量） |
| 摩擦 | 8 项发现（F1-F8）；人工介入 1 次（设计批准，AskUserQuestion） |
| 编排 | N/A（无并行场景） |
| 验证 | 无重复执行；测试漏选 0 |
| 资产 | knowledge_candidates 步骤 ok=true（候选数未查） |
| 维护 | 见 F1-F8 |

## 6. 结论（单次运行，不可外推）

流程维护时间的最大头不是协调命令本身（~18.5 s），而是**失败重试的排障成本**（F1 worktree 不兼容 + F2 project_id + F3 错误信封缺定位 ≈ 25-40 min 模型时间）。这印证了提案 §9.2 的假设：仪式成本主要来自「契约不可发现」而非「命令慢」。
