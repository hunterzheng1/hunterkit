# 批次 1 试点采集记录：组 3（候选方案 harness_task.py）T5 迁移/schema 变更 · 第 1 次运行

> 日期：2026-09-08
> 工作区：`E:\MyProject\AI Related\batch1-pilot-clones\g3-t5-r1`（fresh clone @ 898c8ed + env 提交，detached HEAD）
> 任务：T5（`docs/harness-improvement-roadmap/batch0/tasks/T5-migration-schema.md`）：runtime/invalidations 回执增加可选 `triggerPhase` 字段 + 旧/新双 fixture 兼容读取测试
> 候选方案：`harness_task.py begin/finish/status` 轻任务闭环（efb6f13：含 P1/P5/P6 验证计划）
> 运行身份：runId `task_144995c9-3b20-411f-af1d-d7ac9fdb3fe8`，change `batch0-g3-t5-trigger-phase`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：`trigger_phase` 可选参数贯通（API + CLI）；3 个新测试（旧 fixture 读取不变/新回执含字段且 schemaVersion 不变/不传时无字段）；套件 26/26 绿 |
| 提交 | `94bed54`（2 文件：harness_fixback.py +17、test_harness_fixback.py +194） |
| 归档 | `.harness/archive/2026-09-08-batch0-g3-t5-trigger-phase`（record-only） |
| 档位 | **standard（classify 自动判定）——但任务定义标注 full** |
| 端到端墙钟 | **~14 min**（begin 10:05:17 → 归档完成 10:19:05；含实现与测试编写） |
| finish 成功执行 | 4.42 min（265,098 ms） |
| 失败重试 | 0 次（begin/finish 均一次成功） |

## 2. 命令级计时

### 流程维护命令（协调开销，§4 口径）

| 步骤 | 耗时 |
|---|---|
| harness_task.py begin | ~0.9 s |
| harness_task.py finish（成功） | 265.1 s 总计，其中验证 259.7 s |
| **流程维护净耗时（finish − 验证）** | **~5.4 s** |
| **流程维护合计** | **~6.3 s** |

### 验证命令（不计入流程维护）

| 验证项 | 解析为 | 耗时 |
|---|---|---|
| compile | unitTestFull（npm lint+typecheck+test+build 链，fallback） | 258.1 s |
| unitTest | **定向 python -m unittest test_harness_fixback（python-targeted）** | 1.6 s |
| unitTestFull | DEDUPED（dedupedFrom: compile） | 0 s |
| **验证合计** | | **259.7 s（4.33 min）** |

P1/P5/P6 修复效果在本任务复现：验证从修复前预期的 3× npm 链（~774 s）降为
1× npm 链 + 1× 定向 python（259.7 s，-66%）。

## 3. 关键发现：P12 风险升级盲区

**任务定义标注 full 档（状态 schema 变更 + 兼容读取，含 migration 信号），
但轻任务入口未拒绝，按 standard 跑完。**

classify 的档位判定依据是路径 marker（auth/security/migration/concurrency/
artifact-protocol/shared-state/delete 关键词匹配 productPaths）：

- 实际 diff 路径：`harness/scripts/harness_fixback.py`、
  `harness/scripts/tests/test_harness_fixback.py`
- 路径中不含任何 full marker → signals=['production-code'] → standard

**根因**：路径启发式无法识别「schema 变更」语义——变更的是
`runtime/invalidations/` 回执的 schema（写入方在 harness_fixback.py），
但 diff 路径只显示 Python 文件名。migration marker 匹配的是路径中的
`migration`/`migrate`/`/sql/`/`.sql` 字样，与「JSON 回执加可选字段」
无路径交集。

**影响评估**：
- 产物质量无回退：26/26 测试绿、验收全过、兼容语义正确（旧 fixture
  读取不变实证）——本任务的 full 档要求（独立审查、恢复演练）在
  record-only 轻任务路径下本来就不执行，走完整流程也只是多一道评审。
- 但**风险升级有效性未达成**（提案 §10 批次 1 完成证据的第二项）：
  真正危险的 schema 变更（删字段、改语义）若以类似路径呈现，同样会
  漏判。T4 的备注（「涉及 artifact-protocol 邻接，若 classify 升档按
  升档后流程执行——这本身是测量点」）预示了同一测量需求。

**修复方向**（候选方案改进点，非阻断）：
1. 短期：任务入口接受显式 `--tier full` 声明（用户/上游知道任务是
   schema 变更时直接走完整流程，不依赖路径推断）；
2. 中期：classify 增加 schema 邻接路径规则（`runtime/`、`state/`、
   契约文件清单）作为 full 信号源；
3. 长期：diff 内容语义分析（识别 schemaVersion 字段变更）——成本高，
   需先证明路径规则的召回不足。

## 4. 与 g2（当前 Harness）对照

| 指标 | g2（基线，无 T5 样本） | g3-r1（本运行） |
|---|---|---|
| 端到端墙钟 | 未采集（T5 未跑 g2） | ~14 min |
| 流程维护 | 未采集 | ~6.3 s |
| 档位判定 | 手工 configure-plan（可声明 full） | 自动 standard（**漏判 full**，P12） |

## 5. 备注

- 本运行是 P1/P5/P6 修复（efb6f13）后的第 3 次端到端验证（T1-r3、T3-r3
  之后），定向验证与去重行为稳定复现。
- T5 任务定义的「独立验收保留项」（缺字段≠损坏：损坏 JSON 仍走原有失败
  路径）由既有 `_read_objects` 的 JSONDecodeError 跳过语义满足——
  `test_old_receipt_without_trigger_phase_reads_unchanged` 实证了读取路径。
