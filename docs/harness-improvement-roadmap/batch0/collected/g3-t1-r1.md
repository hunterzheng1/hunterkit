# 批次 1 试点采集记录：组 3（候选方案 harness_task.py）T1 文档修改 · 第 1 次运行

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch1-pilot-clones\g3-t1-r1`（fresh clone @ 898c8ed + env 提交，detached HEAD）
> 任务：T1（`docs/harness-improvement-roadmap/batch0/tasks/T1-docs-edit.md`）
> 候选方案：`harness_task.py begin/finish/status` 轻任务闭环（begin 建档+脏基线 → 自由编辑 → finish 分类/验证/提交/归档一条龙）
> 运行身份：runId `task_*`（begin 生成），change `batch0-g3-t1-docs-fix`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：两处「10 项」→「12 项」；diff 仅 2 行；范围外零 diff |
| 提交 | `185c074`（task.json 记录） |
| 归档 | `.harness/archive/2026-09-07-batch0-g3-t1-docs-fix`（record-only） |
| 档位 | fast（classify 自动判定，无需手工 configure-plan 降档——对照 g2 的 F5） |
| 端到端墙钟 | **~30.4 min**（22:09:25 begin → 22:39:50 归档完成；含 6 次 finish 失败排障） |
| finish 成功执行 | 4.37 min（261,920 ms） |
| 失败重试 | 6 次 finish 失败（全部为试点环境问题，非候选方案契约问题，见 §3） |

## 2. 命令级计时

### 流程维护命令（协调开销，§4 口径）

| 步骤 | 耗时 | 失败重试 |
|---|---|---|
| harness_task.py begin | 0.85 s | 0 |
| harness_task.py finish（成功那次） | 261.9 s 总计，其中验证 260.4 s | 6（见 §3） |
| **流程维护净耗时（finish 总时 − 验证时）** | **~1.5 s** | — |
| **流程维护合计（begin + finish 净）** | **~2.3 s** | — |

### 验证命令（不计入流程维护）

| 步骤 | 耗时 |
|---|---|
| unitTest（自动解析为 unitTestFull：npm lint+typecheck+test+build 链） | 260.4 s |

### 模型手工产物（仪式写作）

| 产物 | 耗时 |
|---|---|
| 无（begin 参数即全部仪式输入：--goal + --acceptance ×3） | ~0 |

## 3. 失败与摩擦明细（全部为试点环境问题）

| # | 事件 | 原因 | 解决 |
|---|---|---|---|
| 1-2 | finish ×2 失败 `VERIFICATION_TARGET_MISSING` | init 后无 build-profile（未跑 preflight detect） | 运行 `harness_preflight.py detect --project .` 生成 |
| 3 | finish 崩溃（traceback） | 候选脚本首版用裸 subprocess.run 执行 argvTemplate，node 探测的 `&&` 链无法执行 | 修复候选脚本（`_split_shell_chain` + `run_managed_command`），主仓已提交 2cdcd32 |
| 4 | finish 失败 `VERIFICATION_FAILED` | fresh clone 无 node_modules，npm lint 失败 | `npm install`（273 包） |
| 5 | finish 失败 `FOREIGN_PATHS_PRESENT` | npm install 修改了 package-lock.json（begin 后变更、不在脏基线） | 作为 env 变更提交 |
| 6 | （r2 才发现）.claude/skills/scripts 副本过期 | init 部署 bundle 版本覆盖手工拷贝 | 候选脚本需同时拷到 harness/scripts/ 与 .claude/skills/scripts/ |

**关键区分**：6 次失败全部是「试点环境搭建」问题（fresh clone 缺 profile/依赖/脚本部署），**没有一次是候选方案的契约不可发现问题**——对照 g2 首跑的 8 次失败全部是 harness 隐式契约（F1-F4）。环境就绪后 finish 一次成功。

## 4. 与 g2（当前 Harness）对照

| 指标 | g2-r1（首跑） | g3-r1（首跑） | 差异 |
|---|---|---|---|
| 端到端墙钟 | 49.6 min | ~30.4 min | -39% |
| 流程维护命令 | ~18.5 s（12 条命令） | ~2.3 s（2 条命令） | **-88%** |
| 仪式写作 | ~12 min（plan-evidence-input.json 4 场景） | 0（--goal/--acceptance 参数） | **-100%** |
| 失败重试 | 8 次（契约问题） | 6 次（环境问题，可预先消除） | 性质不同 |
| 档位判定 | 手工 configure-plan 降档（F5） | classify 自动 fast | 消除 |

## 5. 新增发现

| # | 发现 | 类型 |
|---|---|---|
| P1 | **验证自动解析过重**：docs-only 变更（fast 档）的 unitTest 自动解析为 unitTestFull（npm lint+typecheck+test+build 全链 260 s），而 g2 手工选择的定向 doc contract 测试仅 1.19 s。fast 档的验证选择策略需要独立设计（如 docs-only → doc contract test） | 候选方案改进点 |
| P2 | finish 墙钟被验证主导：4.37 min 中验证占 99.4%，流程维护仅 ~1.5 s | 观察 |

## 6. 与方法学 §4 的映射

| 维度 | 本次数据 |
|---|---|
| 结果 | 验收通过；0 缺陷；0 返工 |
| 时间 | 端到端 ~30.4 min（含环境排障）；流程维护 ~2.3 s |
| 成本 | 未采集 |
| 摩擦 | 6 次失败（全为环境问题）；0 契约问题 |
| 验证 | 无重复执行；测试漏选 0（但选择过重，P1） |
| 资产 | knowledge_candidates 1 条；ledger 1 条（unitTestFull OK） |
