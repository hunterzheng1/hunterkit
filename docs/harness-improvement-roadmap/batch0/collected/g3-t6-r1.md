# 批次 1 试点采集记录：组 3（候选方案 harness_task.py）T6 中断恢复 · 第 1 次运行

> 日期：2026-09-08
> 工作区：`E:\MyProject\AI Related\batch1-pilot-clones\g3-t6-r1`（fresh clone @ 898c8ed + env 提交，detached HEAD）
> 任务：T6（`docs/harness-improvement-roadmap/batch0/tasks/T6-interrupt-recovery.md`）：从中断状态恢复并完成 T3 型交付，不重跑已通过的验证、无重复副作用
> 候选方案：`harness_task.py begin/finish/status` 轻任务闭环（efb6f13：含 P1/P5/P6 验证计划）
> 运行身份：change `batch0-g3-t6-interrupt`（主样本）+ `batch0-g3-t6-deep/deep2/deep3`（深中断窗口探测）

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | **部分通过**：恢复路径可用（中断 → 重跑 finish → 一次成功）；events/ledger/收据零重复；但「不重跑已通过的验证」**未达成**——finish 无 ledger 复用检查，重跑全量验证 |
| 提交 | `4d2fee7`（interrupt 主样本，3 文件）+ `a40318d`（deep）+ `ffba017`（deep2）+ `8a6bc8b`（deep3） |
| 归档 | 4 个 change 全部 record-only 归档，事件结构完全一致（12 事件：1× task phase.end + 1× archive phase.end） |
| 档位 | standard（4 次运行全部自动判定一致） |
| 端到端墙钟 | 主样本 ~16 min（11:26:27 begin → 11:42:29 归档完成；含 1 次中断 + 恢复重跑） |
| finish 成功执行（恢复跑） | 218.6 s（attempt 3，从 attempt 1 的中断态恢复） |
| 失败重试 | 1 次人为中断（attempt 1，验证中 kill）+ 1 次探测失败（attempt 2，后台进程被工具回收） |

## 2. 中断场景与恢复实测

### 2.1 主样本：验证中中断（attempt 1 → 3）

| 阶段 | 时间 | 状态 |
|---|---|---|
| begin | 11:26:27 | task.json open，2 事件（phase.start + decision） |
| finish attempt 1 | 11:28:54 启动 | npm 链运行中，75 s 处 kill 整个进程树 |
| 中断态 | 11:30:12 | task.json 仍 open；事件仍 2 条（finish 未写任何事件）；evidence/ 空（无 ledger）；gate-policy.json/risk-classification.json 已写（幂等重写无害） |
| finish attempt 3（恢复） | 11:38:46 → 11:42:29 | 一次成功：全量验证 213.4 s + 提交 `4d2fee7` + 归档 |

**恢复语义实测**：
- task.json `status=open` 是恢复入口——finish 重跑不被 `TASK_ALREADY_FINISHED` 挡住
- 事件零重复：中断的 finish 在验证阶段不写事件（phase.end 只在验证全过后写），
  恢复跑写一次；归档事件 12 条与无中断运行完全同构
- ledger 零重复：中断点未落任何 ledger 条目；恢复跑从零写起
- 证据文件零残留：evidence/ 目录在中断后为空，恢复跑的 compile/unitTest 日志
  各一份（`compile-20260908-113847.log`、`unitTest-20260908-114217.log`）

### 2.2 深中断窗口探测（deep/deep2/deep3）：未命中

任务规格的中断点是「ledger 已记录、gate close 尚未运行」。实测该窗口宽度：
compile ledger 条目落盘（~185 s 处）→ targeted python（~6 s）→ unitTest ledger
条目 → plan.md/commit/phase.end——**窗口仅 ~6-10 s**。三次探测（212 s 定时、
212 s 定时、3 s 轮询 + ps 找 pid）全部错过：轮询检测到 ledger 出现时，finish
已在 4 s 内完成剩余全部步骤。三次探测 run 均正常完成（190-199 s），副作用
结构与主样本一致。

**结论**：深中断窗口在轻任务架构下极窄（验证后段是秒级快速步骤），
「ledger 已记录但未关门」的中间态在实践中难以稳定捕获；真正可复现的
中断面是验证执行中（分钟级窗口）。窗口窄本身是架构优点——中间态
驻留时间短。

## 3. 关键发现

### 3.1 P14：恢复 = 全量重验，无证据复用

T6 验收条件 1「不重跑已通过的验证」**未达成**：`_run_verification`
（harness_task.py:763-827）总是执行命令，无 can-reuse/ledger 复用检查。
中断后恢复的成本 = 完整验证成本（本样本 213 s，其中 npm 链 207 s 重复
执行）。

- **影响**：恢复路径正确性无虞（幂等副作用 + 覆盖式 ledger），但成本
  效率损失——中断一次多付一轮全量验证。对照：harness_ledger 自身有
  can-reuse 机制（tests_executed/tests_reused_from 字段），轻任务入口
  未接入。
- **权衡**：接入复用需校验输入集哈希未变（中断后 diff 未动才可复用），
  复杂度中等；轻任务定位是短平快，全量重验的简单性 vs 复用的节省，
  需产品决策。批次 1 记录为候选方案改进点，非阻断。
- **规格偏差说明**：T6 规格假设的恢复机制（`PHASE_CLOSE_RESUMED` 或
  等价物）在轻任务架构中不存在——轻任务的等价语义是「task.json open
  即可重跑」，恢复机制极简但无增量续跑。

### 3.2 中断副作用足迹为零（验证阶段）

验证执行中的中断不留下任何需要清理的状态：无事件、无 ledger、无收据；
gate-policy.json/risk-classification.json 被下次运行幂等重写。恢复操作
就是「重跑同一条命令」，无任何状态修复步骤。这满足 T6 验收条件 2 的
精神（无重复副作用）——因为中断点根本没有副作用落盘。

### 3.3 后台进程管理陷阱（试点方法学）

- Git Bash `$!` 返回 bash 内部 pid，`taskkill //PID` 找不到（需 `ps -W`
  映射 winpid，且 python 进程名匹配不稳定）
- `(cmd &)` 分离子 shell 的进程在工具调用结束后被回收——跨工具调用的
  后台任务必须用 run_in_background
- kill 窗口 < 10 s 时，「检测 → 找 pid → kill」的工具调用链延迟（秒级）
  会错过窗口；需单工具调用内完成 launch-poll-kill 闭环

## 4. 与 g2（当前 Harness）对照

| 指标 | g2（基线，无 T6 样本） | g3-r1（本运行） |
|---|---|---|
| 端到端墙钟 | 未采集（T6 未跑 g2） | ~16 min（含 1 次中断恢复） |
| 恢复机制 | PHASE_CLOSE_RESUMED（execute 续跑） | task.json open 重跑（全量重验） |
| 副作用重复 | 未采集 | 零重复（4 次归档同构） |

## 5. 备注

- 主样本的 Python 侧实现与 T4 相同（`_last_activity_at` helper + 字段 +
  回归测试）；deep/deep2/deep3 各追加 1 个边界测试（空文件 null /
  缺 timestamp 行跳过 / 非 str 类型跳过），套件 45→48 全绿。
- 4 次 finish 的验证结构完全一致（compile→unitTestFull fallback 1× +
  unitTest targeted 1× + DEDUPED），P1/P5/P6 行为在第 6-8 次端到端
  运行中持续稳定。
- attempt 2（11:32:46）因分离子 shell 被工具回收而夭折，未产生任何
  状态变更（task.json open 不变），不计入中断样本。
