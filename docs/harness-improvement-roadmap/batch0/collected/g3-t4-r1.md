# 批次 1 试点采集记录：组 3（候选方案 harness_task.py）T4 跨模块变更 · 第 1 次运行

> 日期：2026-09-08
> 工作区：`E:\MyProject\AI Related\batch1-pilot-clones\g3-t4-r1`（fresh clone @ 898c8ed + env 提交，detached HEAD）
> 任务：T4（`docs/harness-improvement-roadmap/batch0/tasks/T4-cross-module.md`）：把 T3 的 `lastActivityAt` 字段透传到 TS 侧（类型定义 + 契约测试）
> 候选方案：`harness_task.py begin/finish/status` 轻任务闭环（efb6f13：含 P1/P5/P6 验证计划）
> 运行身份：change `batch0-g3-t4-last-activity`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：Python 输出（`_last_activity_at` helper + items 字段）、TS 类型（`ChangeStatusItem`/`ChangeStatusPayload`）、序列化（camelCase 原样透传）三处一致；tsc -b 绿；vitest 13/13（3 新契约测试：非空/null/真实 Python 输出 round-trip）；python 45/45（3 新回归测试） |
| 提交 | `ae6ac26`（5 文件：harness_change.py +30、test_harness_change.py +92、sync.ts +31/-2、sync-process.test.ts +99、hunter-workflow-family.json 快照） |
| 归档 | `.harness/archive/2026-09-08-batch0-g3-t4-last-activity`（record-only） |
| 档位 | **standard（classify 自动判定）——任务定义标注的「artifact-protocol 邻接若升档」未触发** |
| 端到端墙钟 | ~26 min（10:56:26 begin → 11:21:53 归档完成；含实现、跨语言测试编写与 3 轮测试排障） |
| finish 成功执行 | 4.15 min（248,738 ms） |
| 失败重试 | 0 次（begin/finish 均一次成功；实现期 3 轮 vitest 失败为测试自身排障，非流程重试） |

## 2. 命令级计时

### 流程维护命令（协调开销，§4 口径）

| 步骤 | 耗时 |
|---|---|
| harness_task.py begin | ~1 s |
| harness_task.py finish（成功） | 248.7 s 总计，其中验证 244.9 s |
| **流程维护净耗时（finish − 验证）** | **~3.8 s** |
| **流程维护合计** | **~4.8 s** |

### 验证命令（不计入流程维护）

| 验证项 | 解析为 | 耗时 |
|---|---|---|
| compile | unitTestFull（npm lint+typecheck+test+build 链，fallback） | 238.9 s |
| unitTest | **定向 python -m unittest test_harness_change（python-targeted）** | 6.0 s |
| unitTestFull | DEDUPED（dedupedFrom: compile） | 0 s |
| **验证合计** | | **244.9 s（4.08 min）** |

P1/P5/P6 修复效果第 5 次复现（T1-r3、T3-r3、T5-r1、T2-r1 之后）。跨模块
diff（Python + TS）下 ledger 定向项 inputsFiles 精确覆盖 4 个变更源文件
（harness_change.py、test_harness_change.py、sync.ts、sync-process.test.ts）。

## 3. 关键发现

### 3.1 P12 第二数据点：跨模块 artifact-protocol 邻接未升档

任务定义风险档位标注 standard，备注「涉及 artifact-protocol 邻接，若
classify 升档按升档后流程执行——这本身是测量点」。实测：**未升档**。

- diff 路径：`harness/scripts/harness_change.py`、
  `harness/scripts/tests/test_harness_change.py`、
  `packages/cli/src/commands/sync.ts`、
  `packages/cli/test/sync-process.test.ts`
- 逐 marker 核对（harness_gate.py:1502-1510）：artifact/protocol/manifest/
  baseline/state//shared/workflow-policy 均无路径交集 → signals=
  ['production-code'] → standard

与 T5（schema 变更漏判 full）同根因：**路径启发式识别不了「变更的是状态
输出契约」语义**——本任务改的正是 harness_change.py status 的输出 schema
（Python↔TS 的线上契约），但路径上没有任何 full marker。两个数据点合并
结论：P12 不是孤例，是路径启发式的系统性盲区；「风险升级有效性」在
批次 1 的两个测量点（T4/T5）均未达成（正确档位任务不受影响，但该升档
的不升档）。

### 3.2 任务规格的 snake_case 误导（P8 同类，实现者已绕开）

T4 规格说「保持 snake_case 线上契约（如已有 camelCase 投影则补投影）」。
实际线上契约是 camelCase（`changeId`、`reasonCodes`、T3 定义的
`lastActivityAt`）；Python 侧输出也是 camelCase。实现者正确识别了括号里的
让步条款（「如已有 camelCase 投影则补投影」），选择 camelCase 透传 +
类型断言不重命名键（round-trip 测试显式断言无 `last_activity_at` 键）。
规格主句与实际契约相反——任务设计侧的规格漂移，与 g2/g3 T3 的 P8
（createdAt vs timestamp）同模式。

### 3.3 主仓 Python 侧缺字段的发现

T4 规格假设 T3 已交付（「把 T3 的 lastActivityAt 字段透传」），但 T3 是
试点克隆、从未合入主仓——主仓 `harness_change.py` status 输出根本没有
`lastActivityAt`。验收条件 1（三处一致）迫使实现者发现此缺口并最小化
补齐 Python 侧（helper + 字段 + 3 个回归测试），而非只改 TS。任务链的
隐式依赖（T4 依赖 T3 交付物）在试点方法学下不成立——采集记录需注明
本运行的 Python 侧改动等价于重做 T3。

### 3.4 vitest cwd 陷阱（试点方法学）

契约测试通过 `runProcess` 直接 spawn Python：vitest 的 `process.cwd()` 是
repo root（非 packages/cli），脚本相对路径按 `../../harness/...` 拼接会
解析到仓库外（`E:\MyProject\AI Related\harness\...`），Python 退出码 2
（can't open file）且 stderr 为空难定位。修复：按 repo root 拼
`harness/scripts/...`。后续跨语言契约测试需注意 vitest cwd 语义。

## 4. 与 g2（当前 Harness）对照

| 指标 | g2（基线，无 T4 样本） | g3-r1（本运行） |
|---|---|---|
| 端到端墙钟 | 未采集（T4 未跑 g2） | ~26 min |
| 流程维护 | 未采集 | ~4.8 s |
| 档位判定 | 手工 configure-plan | 自动 standard（正确档位；artifact-protocol 邻接未升档，P12） |

## 5. 备注

- 本运行是 P1/P5/P6 修复（efb6f13）后的第 5 次端到端验证；跨模块
  （Python+TS）diff 下定向验证与去重行为首次复现，稳定。
- TS 侧类型化不改变运行时行为（`JSON.parse(...) as ChangeStatusPayload`
  仅约束消费方）；`changes` 组件 receipt.details 原样携带 payload。
- hunter-workflow-family.json 快照随提交更新（P9 副作用路径，第 4 次观察）。
