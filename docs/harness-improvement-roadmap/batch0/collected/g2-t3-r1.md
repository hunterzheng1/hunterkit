# 批次 0 采集记录：组 2（当前 Harness）T3 普通功能 · 第 1 次运行

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch0-clones\g2-t3-r1`（fresh clone @ 898c8ed，detached HEAD）
> 任务：T3（普通功能）：`harness_change.py status` 条目新增 `lastActivityAt` 字段（events.ndjson 最后一条事件的 createdAt，无事件为 null）+ 回归测试
> 运行身份：runId `plan_d62eaaf9-9fca-4985-aff4-e05bdeb600ff`，project_id `prj_batch0-g2-t3-r1`，change `batch0-g2-t3-status-field`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：`_last_activity_at` helper + `lastActivityAt` 字段；4 个新测试（末条事件/无事件/文件顺序非最大时间戳/跳过尾部坏行）；套件 46/46 绿 |
| 提交 | `1a2d441`（2 文件，+90 行） |
| 归档 | `.harness/archive/2026-09-07-batch0-g2-t3-status-field`（record-only，1 条 warning：record-only 非 release eligible） |
| 端到端墙钟 | **15.55 min** |
| 活动执行（harness 口径） | **14.66 min**（plan 10.64 + execute 3.99 + archive 0.02） |
| 失败重试 | 2 次（均在 plan publish） |

## 2. 阶段级计时

| 阶段 | 活动时间 | 说明 |
|---|---|---|
| plan | 10.64 min | 含 2 次 publish 失败调试 + plan-evidence-input.json 手工编写（4 场景 × 完整契约字段） |
| execute | 3.99 min | 补丁编写（helper + 字段 + 4 测试）、test_runner 46/46、ledger ×3、gate close |
| archive | 0.02 min | handoff + begin + commit + archive execute（record-only） |

## 3. 失败与摩擦明细

| # | 事件 | 原因 | 解决 |
|---|---|---|---|
| 1 | plan publish 第 1 次失败 | `coverage_dimension: "regression"` 不在枚举内（仅 8 个允许值） | 改为 `integration_impact` |
| 2 | plan publish 第 2 次失败 | `PLAN_ARTIFACT_REFERENCE_INVALID`：approved_scopes（1 条）与 intent.in_scope（2 条）不等 | 按 in_scope 文本逐条生成 approved_scopes |

**与 T1 的关键差异**：T1 的 in_scope 与 approved_scopes 恰好 1:1 所以意外通过；T3 有 2 条 in_scope 才暴露该隐式契约。**F11 是任务规模敏感的契约**——任务越大越容易触发。

## 4. execute 阶段观察（与 T1 对照）

- **PHASE_CLOSED 干净关闭，无场景覆盖 WARN**：T1 的 DOC-002/003 WARN（F7）源于 diff_review 类场景没有 ledger 路径；T3 的 4 个场景全部有 ledger 条目对应，零 WARN。**F7 的边界更清晰了：不是所有场景都缺路径，是 diff_review 类场景缺。**
- ledger ×3（compile/unitTest/unitTestFull）一次成功——契约知识从 T1 会话延续，无试错。

## 5. 新增发现

| # | 发现 | 类型 |
|---|---|---|
| F11 | approved_scopes[].text 必须与 intent.in_scope 排序相等（plan-artifacts validator）；无文档提示，靠报错发现；任务 in_scope 条目数 >1 时必触发 | 契约 |
| F12 | coverage_dimension 是 8 值封闭枚举（business_rules/concurrency_idempotency/data_compatibility/error_codes/integration_impact/normal_path/parameter_validation/permission_boundaries）；"regression" 等直觉值会被拒 | 契约 |

## 6. 与方法学 §4 的映射

| 维度 | 本次数据 |
|---|---|
| 结果 | 验收通过；0 缺陷；0 返工 |
| 时间 | 端到端 15.55 min；活动 14.66 min；plan 占 73%（契约发现 + 仪式写作） |
| 成本 | 未采集 |
| 摩擦 | 2 次失败重试（均 plan publish 契约）；0 人工介入 |
| 验证 | 无重复执行；测试漏选 0（46/46 全跑） |
| 资产 | knowledge_candidates ok |
