# 批次 0 采集记录：组 2（当前 Harness）T3 普通功能 · 第 2 次运行（可重复性验证）

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch0-clones\g2-t3-r2`（fresh clone @ 898c8ed，detached HEAD）
> 任务：T3（同 r1）
> 运行身份：runId `plan_b76eb858-0541-4a40-94c9-a062e0a89116`，project_id `prj_batch0-g2-t3-r2`，change `batch0-g2-t3-status-field`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：与 r1 完全一致（diff 逐字节一致，FULL-DIFF-IDENTICAL） |
| 提交 | `2c6c1a6`（2 文件，+90 行，内容与 r1 的 `1a2d441` 逐字节一致） |
| 归档 | `.harness/archive/2026-09-07-batch0-g2-t3-status-field`（record-only，同 1 条 warning） |
| 端到端墙钟 | **3.22 min**（16:58:12 clone → 17:00:48 execute 关闭）+ 5.27 min 会话中断（bash 语法错误 + 上下文摘要）+ 0.62 min 归档 = 9.11 min 总跨度 |
| 活动执行（harness 口径） | **3.11 min**（plan 0.83 + execute 1.68 + archive 0.59） |
| 失败重试 | 0 次（harness 命令层面）；1 次 bash 语法错误（非 harness 契约） |

## 2. 阶段级计时

| 阶段 | 活动时间 | 说明 |
|---|---|---|
| plan | 0.83 min | plan-evidence-input.json 复用 r1 模板（改 runId/project_id/changeName），publish 一次成功 |
| execute | 1.68 min | 补丁从 r1 复用（90 行插入）、test_runner 46/46、ledger ×3、gate close |
| archive | 0.59 min | handoff + begin + commit + archive execute（record-only） |

## 3. r1 vs r2 对照（可重复性结论）

| 指标 | r1（首跑） | r2（契约已知） | 差异 |
|---|---|---|---|
| 端到端墙钟（不含会话中断） | 15.55 min | 3.22 min | **-79%** |
| 活动执行 | 14.66 min | 3.11 min | -79% |
| plan 阶段 | 10.64 min | 0.83 min | -92% |
| 失败重试 | 2 | 0 | -100% |
| plan publish 尝试 | 3 | 1 | -67% |
| 产出 | +90 行 | +90 行（逐字节一致） | 0 |

**结论**：
1. **T3 可重复性成立**：同任务、同起始 commit、同验收，产出逐字节一致。
2. **首跑成本同样是学习成本**：r1 的 plan 阶段 10.64 min 中，2 次 publish 失败（F11/F12 契约发现）+ 首次编写 4 场景 plan-evidence-input 占绝大部分；r2 复用模板后 plan 仅 0.83 min。
3. **模板复用是当前最强的成本压缩手段**：无需任何 harness 改动，仅靠「上次运行的 plan-evidence-input.json 改 3 个 id」就把 plan 从 10.64 min 压到 0.83 min。这直接支持提案中「知识管线/资产复用」方向，也说明 F10 的量化：仪式写作成本在模板在手时趋近于零，首跑时是主要成本。

## 4. 新增发现

| # | 发现 | 类型 |
|---|---|---|
| F13 | 归档链的 attempt 1（34.6 s）因会话中断未正常关闭，attempt 2（0.75 s）恢复完成——`recoveredMs` 机制正常工作，中断不丢数据 | 观察（正面） |

## 5. 与方法学 §4 的映射

| 维度 | 本次数据 |
|---|---|
| 结果 | 验收通过；0 缺陷；0 返工 |
| 时间 | 端到端 3.22 min（不含中断）；活动 3.11 min |
| 成本 | 未采集 |
| 摩擦 | 0 次 harness 失败重试；1 次 bash 语法错误（采集方自身，非 harness） |
| 验证 | 无重复执行；测试漏选 0（46/46 全跑） |
| 资产 | knowledge_candidates ok |
