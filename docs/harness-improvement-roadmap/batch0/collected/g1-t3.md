# 批次 0 采集记录：组 1（原生 Agent）T3 普通功能 · 2 次运行

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch0-clones\g1-t3-r1`、`g1-t3-r2`（fresh clone @ 898c8ed，detached HEAD，**无 harness init**）
> 对照口径：同 g1-t1——无 Harness Skill、无流程命令；会话级墙钟计时。
> 知识状态：与组 2 T3-r2 同等热缓存（同会话、任务契约与实现方案已知）。

## 1. 结果总览

| 运行 | 端到端墙钟 | 步骤 | 失败 | 提交 | 验收 |
|---|---|---|---|---|---|
| r1 | **2.95 min**（176.7 s） | 读 classify_changes → 写 helper + 字段 → 写 4 测试 → 套件 46/46 → commit | 0 | `d0a3b0f` | 通过 |
| r2 | **1.29 min**（77.1 s） | 同上（方案已知，直接落笔） | 0 | `03c15f8` | 通过 |

r1 与 r2 产出逐字节一致（+87 行）。与组 2 的 diff（+90 行）**语义等价但非逐字节一致**：核心 helper 与字段完全相同，测试风格不同（g2 复用 alpha fixture 写 4 场景，g1 用独立目录建 4 场景），均 46/46 绿。

## 2. 独立验收（评估侧保留项）

乱序时间戳场景（评估侧构造，实现者不可见）：events.ndjson 末条时间戳早于首条，验证取「文件末条」而非「最大时间戳」——两次运行均 PASS（返回 `2026-09-01T08:00:00+00:00` 而非 `2026-09-07T23:59:00+00:00`）。

## 3. 观察要点

1. **零失败、零契约排障**：组 2 T3-r1 在 plan publish 上花掉的 2 次失败重试（F11 approved_scopes 等价、F12 coverage_dimension 枚举）在原生组不存在——这些是 Harness 自身引入的契约，不是任务本身的复杂度。
2. **测试编写是原生组的主要时间构成**（r1 的 2.95 min 中约 60%）：与任务本质复杂度相关，两组共有。
3. **产出自然变异**：同任务两次独立实现（g1 vs g2）语义等价但代码风格不同——说明「逐字节一致」不是质量的必要条件；Harness 模板复用带来的逐字节一致是复用机制的副产品，不是质量优势本身。

## 4. 与方法学 §4 的映射

| 维度 | r1 / r2 |
|---|---|
| 结果 | 验收通过（含乱序时间戳保留场景）；0 缺陷；0 返工 |
| 时间 | 端到端 2.95 / 1.29 min；流程维护 0 |
| 成本 | 未采集 |
| 摩擦 | 0 失败；0 人工介入 |
| 验证 | 46/46 全跑（两次）；测试漏选 0 |
| 资产 | 无（同 g1-t1：原生组不产生知识候选） |
