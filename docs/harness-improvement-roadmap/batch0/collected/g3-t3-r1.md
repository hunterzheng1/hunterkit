# 批次 1 试点采集记录：组 3（候选方案 harness_task.py）T3 普通功能 · 第 1 次运行

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch1-pilot-clones\g3-t3-r1`（fresh clone @ 898c8ed + env 提交，detached HEAD）
> 任务：T3（`docs/harness-improvement-roadmap/batch0/tasks/T3-ordinary-feature.md`）：`harness_change.py status` 条目新增 `lastActivityAt` 字段 + 回归测试
> 候选方案：`harness_task.py begin/finish/status` 轻任务闭环
> 运行身份：runId `task_3894f7bf-aa72-4fab-8ae5-c755f06bf92f`，change `batch0-g3-t3-status-field`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：`_last_activity_at` helper + `lastActivityAt` 字段；4 个新测试（末条事件/无事件/文件顺序非最大时间戳/跳过尾部坏行）；套件 46/46 绿；独立保留检查（乱序时间戳取最后一条）通过 |
| 提交 | `2cc2c2b`（3 文件，+118 行：harness_change.py +31、test +86、hunter-workflow-family.json hash +1） |
| 归档 | `.harness/archive/2026-09-07-batch0-g3-t3-status-field`（record-only） |
| 档位 | standard（classify 自动判定） |
| 端到端墙钟 | **21.52 min**（begin 23:05:48 → 归档完成；含 0 次失败重试） |
| finish 成功执行 | 11.43 min（685,650 ms） |
| 失败重试 | 0 次（begin/finish 均一次成功） |

## 2. 命令级计时

### 流程维护命令（协调开销，§4 口径）

| 步骤 | 耗时 |
|---|---|
| harness_task.py begin | 0.91 s |
| harness_task.py finish（成功） | 685.7 s 总计，其中验证 679.1 s |
| **流程维护净耗时（finish − 验证）** | **~6.5 s** |
| **流程维护合计** | **~7.4 s** |

### 验证命令（不计入流程维护）

| 验证项 | 解析为 | 耗时 |
|---|---|---|
| compile | unitTestFull（npm lint+typecheck+test+build 链） | 248.5 s |
| unitTest | unitTestFull（同上） | 189.9 s |
| unitTestFull | unitTestFull（同上） | 240.8 s |
| **验证合计** | | **679.1 s（11.32 min）** |

### 模型手工产物（仪式写作）

| 产物 | 耗时 |
|---|---|
| 无（--goal + --acceptance ×3 参数即全部） | ~0 |

## 3. 与 g2（当前 Harness）对照

| 指标 | g2-r1（首跑） | g3-r1（首跑） | 差异 |
|---|---|---|---|
| 端到端墙钟 | 15.55 min | 21.52 min | **+39%（更慢）** |
| 流程维护 | ~2.2 min 稳态口径（首跑另加契约排障 ~7 min + 仪式写作 ~8 min） | **~7.4 s** | **-94%（vs 稳态口径）** |
| 失败重试 | 2 次（F11/F12 契约） | 0 次 | -100% |
| 档位判定 | 手工 configure-plan | classify 自动 standard | 消除 |
| 验证 | test_runner 46/46（定向 Python 测试，~15 s） | npm 全链 ×3（11.32 min） | **+45 倍** |

## 4. 新增发现

| # | 发现 | 类型 |
|---|---|---|
| P5 | **standard 档验证三连跑同一命令**：compile/unitTest/unitTestFull 三项全部解析为同一条 npm 链（build-profile 只有 unitTestFull 一个 target），等效同一验证跑 3 遍（679 s vs 单次 ~240 s）。候选方案应对 resolvedAs 相同的验证项去重 | 候选方案缺陷 |
| P6 | **验证粒度错配**：任务只改了 Python（harness_change.py），但 node 探测的 profile 只有 npm 链；g2 的 test_runner 能定向跑 Python 测试（46/46，~15 s）。候选方案缺「按变更语言/路径选验证」的机制 | 候选方案改进点 |
| P7 | **npm test 的 pretest 副作用**：`npm test` 触发 `pretest → sync:harness`，把 harness/ 源码（含候选脚本本身）同步进 bundle manifest，产生 hunter-workflow-family.json hash 变更——被正确识别为任务工作（非 foreign），但意味着「部署候选脚本」这个 env 动作会渗入任务提交 | 观察（试点方法学） |
| P8 | 任务规格说 events.ndjson 取 `createdAt`，但实际事件 schema 字段是 `timestamp`——按规格实现的 lastActivityAt 对真实 change 恒为 null。规格与 schema 的漂移是任务设计侧问题（g2 同样按规格实现，行为一致） | 任务规格问题 |

## 5. 与方法学 §4 的映射

| 维度 | 本次数据 |
|---|---|
| 结果 | 验收通过（含独立保留检查）；0 缺陷；0 返工 |
| 时间 | 端到端 21.52 min；流程维护 ~7.4 s；验证 11.32 min（占 finish 99%） |
| 成本 | 未采集 |
| 摩擦 | 0 次失败重试；0 契约问题 |
| 验证 | 无重复执行（但同链三连跑，P5）；测试漏选 0 |
| 资产 | knowledge_candidates 1；ledger 3 条（compile/unitTest/unitTestFull 全 OK） |

## 6. 单次运行结论

候选方案把流程维护从 g2 稳态的 ~2.2 min 压到 **~7.4 s（-94%）**，且首跑零契约失败——试点门槛「流程维护 ≤1.1 min」大幅达成。但端到端墙钟反超 g2（21.52 vs 15.55 min），瓶颈完全在验证策略（P5 三连跑 + P6 粒度错配），两者都是候选方案可修复的已知问题，不是架构性障碍。
