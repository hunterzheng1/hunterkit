# 批次 0 采集记录：组 2（当前 Harness）T1 文档修改 · 第 2 次运行（可重复性验证）

> 日期：2026-09-07
> 工作区：`E:\MyProject\AI Related\batch0-clones\g2-t1-r2`（fresh clone @ 898c8ed，detached HEAD）
> 任务：T1（同 r1）
> 运行身份：runId `plan_caf5ace6-9c6f-4b7f-b485-b46ea67ba5e5`，attempt 1，change `batch0-g2-t1-docs-fix`

## 1. 结果

| 项 | 值 |
|---|---|
| 验收 | 通过：与 r1 完全一致（两处 10→12、doc contract 8/8、diff 2 行、范围外零 diff） |
| 提交 | `4cb3942`（内容与 r1 的 `22a3d89` 逐字节一致） |
| 归档 | `.harness/archive/2026-09-07-batch0-g2-t1-docs-fix`（record-only，同 1 条 warning） |
| 端到端墙钟 | **5.41 min**（16:24:35 → 16:30:00） |
| 活动执行（harness 口径） | **4.57 min**（plan 1.88 + execute 2.69 + archive 0.39） |
| 人工等待 | 0.84 min（agentOrToolUnattributedMs 50,527 ms） |

## 2. 命令级计时（全部一次成功，零失败重试）

| 步骤 | 耗时 | 失败 |
|---|---|---|
| fresh clone + checkout | 0.52 s | 0 |
| harness init | 3.11 s | 0 |
| bootstrap-plan | 1.36 s | 0 |
| configure-plan | 0.18 s | 0 |
| plan-evidence-input.json（复用 r1 模板改 2 个 id） | ~1 min（模型时间） | 0 |
| plan publish | 2.08 s | **0**（r1 为 5 次尝试） |
| claim + close plan + handoff + begin execute（4 步链） | 1.62 s | **0**（r1 为 3 次失败往返） |
| 两处编辑（Edit ×2） | ~30 s（模型时间） | 0 |
| doc contract test（test_runner exec） | 1.61 s | 0 |
| ledger record ×3（compile/unitTest/unitTestFull） | 8.79 s | **0**（r1 为 4 次试错） |
| gate close execute | 10.55 s | 0 |
| handoff + begin archive + commit（3 步链） | 0.80 s | 0 |
| archive execute | 1.42 s | 0 |

**流程维护命令累计：~32.4 s（12 条命令，零失败）**
**失败重试：0 次（r1 为 8 次）**

## 3. r1 vs r2 对照（可重复性结论）

| 指标 | r1（首跑） | r2（契约已知） | 差异 |
|---|---|---|---|
| 端到端墙钟 | 49.6 min | 5.41 min | **-89%** |
| 活动执行 | 32.26 min | 4.57 min | -86% |
| 流程维护命令时间 | ~18.5 s | ~32.4 s | +75%（多跑 3 条 ledger，r1 部分失败未计） |
| 失败重试次数 | 8 | 0 | -100% |
| plan publish 尝试 | 5 | 1 | -80% |
| 人工介入 | 1（设计批准） | 0（沿用 r1 已批准设计） | -1 |
| 产出 | 2 行 diff | 2 行 diff（逐字节一致） | 0 |

**结论**：
1. **采集链路可重复**：同任务、同起始 commit、同验收，产出逐字节一致；三阶段 WARN 模式一致（DOC-002/003 场景覆盖缺口在两次运行中均复现——F7 是系统性缺口而非偶发）。
2. **首跑成本是学习成本**：89% 的墙钟差异来自契约发现（F1-F4 排障）。同一会话/同一知识状态下，当前 Harness 的稳态流程维护成本约 32 s 命令时间 + ~1.5 min 模型仪式写作（plan-evidence-input）。
3. **对批次 0 度量的含义**：冷缓存（新会话、无知识命中）与热缓存（同会话第二个任务）必须分开统计（方法学 §3 已预见）；r2 类似热缓存但更优（任务也相同）。真正的冷缓存对照要留给组 1 与新会话组 2。

## 4. 新增发现

| # | 发现 | 类型 |
|---|---|---|
| F9 | 稳态下流程维护命令总耗时 ~32 s，其中 gate close execute（10.5 s）与 ledger ×3（8.8 s）占 60%——两项都是 docs-only 变更的冗余仪式（F6 的量化佐证） | 量化 |
| F10 | plan-evidence-input.json 是最大的模型手工产物（~1 min 稳态，~12 min 首跑）；模板复用可压缩，但字段契约（scenario 枚举、hash、scope 等价）仍需人工保证 | 量化 |

## 5. 与方法学 §4 的映射

| 维度 | 本次数据 |
|---|---|
| 结果 | 验收通过；0 缺陷；0 返工 |
| 时间 | 端到端 5.41 min；活动 4.57 min；流程维护 ~32.4 s 命令 + ~1.5 min 仪式写作 |
| 成本 | 未采集 |
| 摩擦 | 0 次失败重试；0 人工介入 |
| 验证 | 无重复执行；测试漏选 0 |
| 资产 | knowledge_candidates ok（同 r1） |
