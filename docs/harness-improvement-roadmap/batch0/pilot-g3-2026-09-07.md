# 批次 1 试点数据：组 3（候选方案 harness_task.py）vs 基线

> 日期：2026-09-07（T1/T3）· 2026-09-08（T2/T4/T5/T6 + P1/P5/P6 修复复跑）
> 样本：8 次运行（组 3：T1×2 + T3×2 + T2/T4/T5/T6 各 1），对照 `baseline-data-2026-09-07.md`（组 2 Harness / 组 1 原生）
> 候选方案：`harness_task.py begin/finish/status` 轻任务闭环（begin 建档+脏基线 → 自由编辑 →
> finish 一条龙：classify 定档 → 验证 → 提交 → 归档）
> 试点门槛（方法学 §6.6）：流程维护 ≤1.1 min（-50%）；T1 e2e ≤4.3 min（-20%）；质量零回退。

## 1. 端到端墙钟总表

| 任务 | g2-r1（基线首跑） | g2-r2（基线稳态） | g3-r1（试点首跑） | g3-r2（试点稳态） | g1 最好值 |
|---|---|---|---|---|---|
| T1 文档修改 | 49.6 min | 5.41 min | ~30.4 min¹ | 8.2 min² | 0.82 min |
| T3 普通功能 | 15.55 min | 3.22 min | 21.52 min | 30.46 min³（纯成功路径 ~8.6） | 1.29 min |
| T2 状态输出扩展 | 未采集 | 未采集 | ~11 min（2026-09-08） | — | — |
| T4 跨模块变更 | 未采集 | 未采集 | ~26 min（2026-09-08） | — | — |
| T5 schema 变更 | 未采集 | 未采集 | ~14 min（2026-09-08） | — | — |
| T6 中断恢复 | 未采集 | 未采集 | ~16 min（2026-09-08，含 1 次中断恢复） | — | — |

¹ 含 6 次 finish 失败排障（全部为试点环境问题：fresh clone 缺 profile/node_modules/脚本部署）。
² 含 1 次失败（.claude 副本过期）。
³ 含 2 次失败（1 次 flaky 测试 + 1 次 P9 候选缺陷）。

注：T2/T4/T5/T6 的 g3 墙钟含实现与测试编写时间（模型手工产物），且在
P1/P5/P6 修复（efb6f13）后的脚本上运行——验证已是最优路径（1× npm 链 +
定向验证），流程维护均为秒级（见 §3）。

## 2. 试点门槛判定

| 门槛 | 目标 | 实测 | 判定 |
|---|---|---|---|
| 流程维护 | ≤1.1 min（-50%） | **T1-r2 ~8.6 s；T3-r1 ~7.4 s；T3-r2 ~3.6 s；T2 ~5.5 s；T4 ~4.8 s；T5 ~6.3 s；T6 ~4.5 s**（-93%~-98%） | **达成（大幅超出，8 次运行一致）** |
| T1 e2e | ≤4.3 min（-20%） | T1-r2 8.2 min → **P1/P5/P6 修复后 r3 ~2.5 min**（finish 1.1 s） | **达成**（修复后复跑验证） |
| 质量零回退 | 0 新失败 | 8/8 验收通过（含独立保留检查）；T3 产出逐字节一致；T1 产出逐字节一致；T2/T4/T5/T6 套件全绿（45-48 python + 13 vitest） | **达成**（P9 是流程缺陷非质量缺陷，且已修复） |

## 3. 流程维护拆分（方法学 §4 口径）

| 构成 | g2 稳态（基线） | g3（候选方案） |
|---|---|---|
| 协调命令 | ~32.4 s（12 条） | ~3-7 s（2 条：begin + finish） |
| 仪式写作 | ~1.5 min（plan-evidence-input 模板复用后） | **0**（--goal/--acceptance 参数） |
| **合计** | **~2.2 min** | **~4-9 s** |
| 首跑额外契约排障 | T1 ~30 min（F1-F4）；T3 ~7 min（F11/F12） | **0**（无隐式契约：错误信封带 field_path + recoveryAction） |
| 首跑仪式写作 | ~12 min（4 场景完整契约） | 0 |

## 4. 候选方案的新瓶颈（P 系列，全部可修复）

| # | 发现 | 影响 | 修复方向 | 状态 |
|---|---|---|---|---|
| P1 | fast 档验证自动解析过重：docs-only 任务跑 npm 全链（260-286 s），g2 手工定向 doc contract 测试仅 1.19 s | T1 e2e 未达门槛的直接原因 | docs-only → doc contract test 的档位感知验证选择 | **已修复**（efb6f13） |
| P5 | standard 档三项验证（compile/unitTest/unitTestFull）全部解析为同一条 npm 链，等效跑 3 遍 | T3 验证 679 s vs 单次 240 s | resolvedAs 相同的验证项去重 | **已修复**（efb6f13） |
| P6 | 验证粒度错配：任务改 Python，验证只有 npm 链（node 探测 profile） | 同上 | 按变更语言/路径选验证（Python → python -m unittest） | **已修复**（efb6f13） |
| P9 | finish 失败重入时，验证副作用文件（npm pretest → sync:harness 改 bundle hash）被误判 foreign | T3-r2 多 1 次重试 | 重入时重建 ownership 契约或副作用白名单 | **已修复**（1fdd770） |
| P10 | flaky 测试放大墙钟：全量链单挂即整个 finish 重来（13.9 min 白跑） | T3-r2 多 13.9 min | P5/P6 修复（定向验证）天然缓解 | 已缓解 |
| P12 | classify 路径启发式识别不了 schema/契约变更语义：T5（回执 schema）与 T4（状态输出契约）均应升 full 档但判 standard | 风险升级有效性未达成（该升不升）；产物质量无回退 | 短期：显式 --tier 声明；中期：schema 邻接路径规则；长期：diff 语义分析 | **已修复**（8397859：CONTRACT_SCHEMA_PATHS 精确清单 + begin --tier floor，批次 2） |
| P13 | 归档 decision 事件文案硬编码 "review missing on full tier"，standard 档也发出，误导审计 | 事件流可读性（无行为差异） | note 带实际 tier 或仅 full 档发 | **已修复**（批次 2：note/reason 读 preflight checks.riskTier；附带修复 gate-policy.json tier 未同步 floor 裁决值） |
| P14 | 中断恢复 = 全量重验：finish 无 ledger 复用检查，恢复成本 = 完整验证成本 | T6 验收条件 1 未达成；中断一次多付一轮验证（~3.5 min） | 接入 can-reuse（输入集哈希未变才复用） | **已决策-不接入**（2026-09-08，批次 2）：主导中断场景（唯一验证执行中）无已落盘证据可复用，收益为 0；唯一全覆盖的深窗口（~6-10 s）实测不可命中；T6 样本本身收益为 0。罕见收益不值得在证据可信底线上换假复用风险面。机制保留：若未来数据显示 finish 重跑（尤其补归档路径）高频，可按 opt-in `--reuse` 落地（decide_can_reuse 已就绪，harness_ledger.py:2037） |

**架构性结论**：候选方案把「流程维护」从 ~2.2 min 压到秒级、把「契约学习成本」清零——
原提案定位的两大成本项都被消除。验证选择策略（P1/P5/P6）修复后，8 次运行
验证结构稳定（docs-only → doc contract 661 ms；Python 变更 → 1× npm 链 +
定向 unittest；同 argv 去重）。P 系列全部关闭（P14 已决策-不接入），
轻任务闭环无遗留开放项。

## 5. 质量与资产对照

| 维度 | g2（基线） | g3（候选） | g1（原生） |
|---|---|---|---|
| 验收通过（含独立保留） | 4/4 | 8/8（T1/T3 ×2 + T2/T4/T5/T6） | 4/4 |
| 产出可重复性 | 逐字节一致 | 逐字节一致（T1、T3 均验证） | 一致 |
| 档位判定 | 手工 configure-plan（F5） | classify 自动（fast/standard 正确；full 该升不升 = P12） | N/A |
| 失败重试性质 | 隐式契约不可发现（F1-F12） | 环境问题 + 已修复的候选缺陷（P9） | 0 |
| 知识候选/ledger/归档 | 有 | 有（自动） | 无 |
| 错误信封质量 | 缺 field_path（F3） | 带 field_path + recoveryAction + problems | N/A |
| 中断恢复 | PHASE_CLOSE_RESUMED 续跑 | task.json open 重跑（全量重验，副作用零重复） | N/A |

## 6. 样本量与不确定性

- 每格 n=2（T1/T3）或 n=1（T2/T4/T5/T6），单 agent 单会话；npm 链耗时波动
  ±27%（P11），验证时间比较需谨慎。
- g3 首跑的「环境问题」6 次失败部分源于试点方法学不成熟（脚本双部署、
  preflight 顺序），真实用户首跑预期介于 r1 与 r2 之间。
- T2/T4/T5/T6 无 g2 对照样本（基线未跑）——成本对比仅限 g3 内部纵向
  （修复前后）与架构指标（流程维护）。
- T6 的深中断窗口（ledger 已记录、未关门）仅 ~6-10 s，三次探测未命中；
  实测的中断面是验证执行中（分钟级）。

## 7. 对批次 1 后续的输入

1. **轻任务闭环架构验证通过**：流程维护 -93%+、契约学习清零、质量零回退——继续推进。
2. **验证选择策略（P1/P5/P6）已修复**（2026-09-08，`efb6f13`）：finish 执行前生成
   变更感知验证计划——docs-only 命中 doc contract 扫描范围 → doc contract 测试；
   harness Python 变更 → 定向 unittest（映射表与 changed-test-selection.mjs 对齐）；
   同 argv 去重（deduped 项不执行不记账）。修复后复跑（r3，fresh clone @ 898c8ed
   + 部署新脚本）：
   - **T1**：finish 1.1 s（验证 661 ms doc contract，对照修复前 261.9 s npm 链）；
     e2e ~2.5 min（begin→编辑→finish，含人工操作）——**T1 e2e 门槛 ≤4.3 min 达成**。
   - **T3**：finish 274 s = npm 链 1×（compile 250.9 s）+ 定向 python 1×（15.9 s）
     + unitTestFull DEDUPED，对照修复前 679 s（3× npm 链），**-60%**；
     一次成功（无重试），npm pretest 副作用文件（hunter-workflow-family.json）
     经 P9 修复路径正常并入提交。
   - ledger 记账：定向项 unitTest=incremental + 显式 files（变更源+测试文件）；
     回退项 unitTestFull=full + profile 输入集——无假证据。
3. **P9（重入 foreign 误判）已修复**（2026-09-07，`1fdd770`）：通道 b 收窄到
   .harness 命名空间越界；契约外产品树脏路径并入 ownership 重新声明。
   单测 + 试点 clone 端到端复现（真实 finish 两轮）双验证。
4. flaky 测试（recovery-v3 120 s 超时）是仓库既有问题，建议独立跟踪。

## 8. T2/T4/T5/T6 试点结果（2026-09-08，全部在 efb6f13 脚本上）

| 任务 | finish | 验证结构 | 档位 | 重试 | 关键发现 |
|---|---|---|---|---|---|
| T2 状态输出扩展 | 247.1 s | npm 1×（237.4 s）+ 定向 python（5.1 s）+ DEDUPED | standard ✓ | 0 | P13（归档事件 tier 措辞误导） |
| T4 跨模块变更 | 248.7 s | npm 1×（238.9 s）+ 定向 python（6.0 s）+ DEDUPED | standard（artifact-protocol 邻接未升档 = P12 第二数据点） | 0 | 主仓 Python 侧缺字段（T3 未合入）；vitest cwd 陷阱 |
| T5 schema 变更 | 265.1 s | npm 1×（258.1 s）+ 定向 python（1.6 s）+ DEDUPED | standard（**任务定义 full，漏判 = P12 首个数据点**） | 0 | 旧/新 fixture 兼容读取实证 |
| T6 中断恢复 | 218.6 s（恢复跑） | npm 1×（206.8 s）+ 定向 python（6.5 s）+ DEDUPED | standard ✓ | 1 次人为中断 | P14（恢复=全量重验）；中断副作用足迹为零；4 次归档事件同构 |

**批次 1 完成证据判定**（提案 §10：真实任务成本下降 + 风险升级有效）：

1. **真实任务成本下降：达成**。8 次运行流程维护全部秒级（-93%+）；
   P1/P5/P6 修复后验证成本稳定在 1× npm 链 + 定向验证（-60%~-66% vs
   修复前 3× npm 链）；T1 e2e 门槛达成；零契约学习成本。
2. **风险升级有效：未达成**（P12）。两个测量点（T4/T5）均未触发升档：
   路径启发式无法识别 schema/契约变更语义。产物质量无回退（该升档的
   任务在 record-only 轻任务路径下本就不执行 full 档的独立审查/恢复
   演练），但「真正危险的 schema 变更若以类似路径呈现会漏判」的风险
   敞口真实存在。修复方向已明确（短期 --tier 声明 / 中期 schema 邻接
   路径规则），建议进批次 2。

