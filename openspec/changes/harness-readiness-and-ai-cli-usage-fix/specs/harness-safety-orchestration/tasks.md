# 实施任务拆解 - harness-safety-orchestration（增量）

> **边界声明**：增量修改，仅涉及 Hook/agent 模板标注、事件路径统一、status 扩展。

---

## 1. 任务总览

### 1.1 关联文档

| 文档 | 路径 | 说明 |
|-----|------|------|
| 技术契约 | `specs/harness-safety-orchestration/spec.md` | 5 需求项 9 场景 |
| 技术方案 | `specs/harness-safety-orchestration/design.md` | 5 文件 5 修改点 |

### 1.2 实现范围

Hook 脚本/配置文件增加模板标注、Subagent 定义增加 `_status: "template"`、事件路径统一到 `.harness/events/`、status/doctor 展示 safety 能力矩阵。

### 1.3 技术栈

TypeScript 5.5+ / Bash / Markdown

---

## 2. 任务执行拓扑图

### 2.0 测试策略

**当前测试策略**：`测试驱动`

### 2.1 拓扑图

```
层级 1: [TASK-SAF-01] 编写 safety 模板标注测试
层级 2: [TASK-SAF-02] Hook 脚本 + 配置文件增加模板标注
        [TASK-SAF-03] Subagent 定义增加 _status: "template"
        [TASK-SAF-04] 事件路径统一到 .harness/events/
层级 3: [TASK-SAF-05] 扩展 status/doctor 展示 safety 矩阵
层级 4: [TASK-SAF-06] 运行测试 + --repair-adapters 验证
```

---

## 3. 原子任务清单

### [TASK-SAF-01] 编写 safety 模板标注测试

- **类型**: 测试-骨架
- **依赖**: 无
- **状态**: [x] 已完成

- **任务描述**: 编写测试验证 Hook/agent 模板标注
- **输出**: `test/safety-template-markers.test.ts`

- **实现步骤**:
  1. 测试 Hook 脚本头部含 "Generated by @hunterzheng/harness"
  2. 测试 settings.json 含 `_generated_by` 字段
  3. 测试 agent 定义含 `_status: "template"`
  4. 测试 status 输出含 safety 能力矩阵

- **验收标准**:
  - [x] 4 个测试用例存在（预期部分失败）

---

### [TASK-SAF-02] Hook 脚本 + 配置文件增加模板标注

- **类型**: 配置
- **依赖**: TASK-SAF-01
- **状态**: [x] 已完成

- **任务描述**: 在 Hook 生成逻辑中增加模板标注注释
- **输入**: `src/capabilities/safety/command.ts` + Hook 模板脚本
- **输出**: 标注后的生成逻辑

- **实现步骤**:
  1. `.sh` 脚本头部增加模板注释（含生成者、触发时机、状态说明）
  2. `settings.json` / `hooks.json` 增加 `_generated_by` + `_generated_at`
  3. 确保 `--repair-adapters` 能基于标注识别和更新

- **验收标准**:
  - [x] 生成的 .sh 脚本首行含模板注释
  - [x] 生成的 JSON 配置文件含 `_generated_by`

- **关联设计**: design.md §4.1 & §4.2

---

### [TASK-SAF-03] Subagent 定义增加 _status: "template"

- **类型**: 配置
- **依赖**: TASK-SAF-01
- **状态**: [x] 已完成

- **任务描述**: 在 19 个 agent 定义文件生成时增加模板状态标记
- **输入**: `src/capabilities/safety/command.ts`
- **输出**: 标注后的生成逻辑

- **实现步骤**:
  1. agent 定义文件 frontmatter/yaml header 增加 `_status: "template"`
  2. 增加 `_note` 说明"本 agent 为模板定义，实际编排后续版本"

- **验收标准**:
  - [x] 生成的 agent 文件含 `_status: "template"`
  - [x] `--repair-adapters` 后 agent 文件保留标注

- **关联设计**: design.md §4.3

---

### [TASK-SAF-04] 事件路径统一到 .harness/events/

- **类型**: 接口层
- **依赖**: TASK-SAF-01
- **状态**: [x] 已完成

- **任务描述**: 新事件只写入 `.harness/events/`，旧路径只读兼容
- **输入**: `src/capabilities/safety/command.ts`
- **输出**: 事件路径修正

- **实现步骤**:
  1. session-summary hook 写入目标改为 `.harness/events/<timestamp>-<type>.json`
  2. 读取事件时兼容 `skywalk-sdd/` 作为只读源

- **验收标准**:
  - [x] 新事件写入 `.harness/events/` 路径
  - [x] 旧 `skywalk-sdd/` 事件可正常读取

- **关联设计**: design.md §2.2

---

### [TASK-SAF-05] 扩展 status/doctor 展示 safety 矩阵

- **类型**: 接口层
- **依赖**: TASK-SAF-02, TASK-SAF-03
- **状态**: [x] 已完成

- **任务描述**: status 命令展示 safety 各功能完成状态，doctor 标注 Hook 模板性质
- **输入**: `src/commands/status.ts`, `src/commands/doctor.ts`
- **输出**: 扩展后的命令

- **实现步骤**:
  1. status 增加 `capabilities.safety` 节点
  2. 展示 Hook installed/template、Dispatcher next_version
  3. doctor 诊断 Hook 时标注模板性质

- **验收标准**:
  - [x] `harness status --json` safety 节点含功能状态矩阵
  - [x] doctor 输出标注 Hook 为模板

- **关联设计**: design.md §6.1 步骤 5

---

### [TASK-SAF-06] 运行测试 + --repair-adapters 验证

- **类型**: 测试-验证
- **依赖**: TASK-SAF-04, TASK-SAF-05
- **状态**: [x] 已完成

- **验收标准**:
  - [x] `test/safety-template-markers.test.ts` 4/4 通过
  - [x] `node dist/bin/harness.js config --repair-adapters` 后标注正确
  - [x] 新事件写入 `.harness/events/`
  - [x] `npm run typecheck` 通过

---

## 4. 交付物

| 文件路径 | 说明 | 对应任务 |
|---------|------|---------|
| `src/capabilities/safety/command.ts` | 模板标注 + 事件路径 | TASK-SAF-02,03,04 |
| `.harness/adapters/**/hooks/*.sh` | Hook 脚本模板注释 | TASK-SAF-02 |
| `.harness/adapters/**/agents/*.md` | agent _status 标注 | TASK-SAF-03 |
| `src/commands/status.ts` | safety 矩阵 | TASK-SAF-05 |
| `src/commands/doctor.ts` | Hook 诊断标注 | TASK-SAF-05 |
| `test/safety-template-markers.test.ts` | 模板标注测试 | TASK-SAF-01 |

---

> **质量红线检查清单**
> - [x] 每个任务 ≤ 5 分钟
> - [x] 100% 覆盖 spec.md（5 需求项 → 6 任务）
> - [x] 100% 覆盖 design.md（5 修改点 → 6 任务）
> - [x] 无循环依赖